# Add a few useful aliases.
alias ..='cd ..'
alias c='clear'
alias l='ls -la'
alias art='php artisan'

# Easy access to Composer package binaries.
export PATH="./vendor/bin:$PATH"

# Navigate to the project root, as that's where we'll be 99% of the time.
cd /var/code
